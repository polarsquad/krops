apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Intentionally empty: no AWS resource-provisioning apps run on the workload
# clusters anymore (issue #346 — the ACK controllers and the Bucket /
# DBInstance / reader Role CRs moved to the management cluster under
# mgmt/aws/infrastructure/). The workload clusters keep their own Flux
# instance, ready for a future application workload (the local-host Podinfo
# pattern), but reconcile nothing today.
resources: []
